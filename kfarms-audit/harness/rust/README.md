### Rust Harness (Skeleton)

Set up a `solana-program-test` harness mirroring the Python model semantics. Add tests that apply randomized sequences and compare on-chain state deltas with the Python oracle.

